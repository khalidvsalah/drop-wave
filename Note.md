- There is a bug in ( props file ) in transform function it doesn't check computedcss translateX | translateY;

// Now

<!-- - Take to long to checkProps. -->
  <!-- - Don't call delay in every play. -->
  <!-- - call Delay just on new prop. -->
